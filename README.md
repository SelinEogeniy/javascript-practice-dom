# javascript-practice-dom

🟢 EASY (x5)

1. Клік — показати текст

На сторінці є кнопка.
При натисканні виведи під нею текст "Clicked!".
Повторні кліки не повинні дублювати текст.

2. Зміна кольору

Є <div id="box"> з розміром 100×100 px.
При кліку на нього змінює колір на випадковий.

3. Лічильник

Створи кнопку "Add".
Поруч показуй число, яке збільшується при кожному кліку.

4. Показати/сховати елемент

Є текстовий блок та кнопка “Toggle”.
Кнопка повинна приховувати/показувати елемент.

5. Зміна тексту

Є <p id="text">Hello</p> і кнопка.
При натисканні змінюй текст на “World”.

🟡 NORMAL (x5)

1. Додавання в список

Є <ul id="items"></ul> + input + кнопка.
При натисканні додавай новий <li> з текстом із input.

2. Видалення елемента

Є список <ul> де кожен <li> має кнопку “Remove”.
При натисканні видаляй саме цей li.

3. Лічильник з двома кнопками

Створи:

кнопку +

кнопку –

span із числом

Клік по + збільшує число, по – зменшує. Нижче 0 йти не можна.

4. Перемикання теми (dark/light)

Кнопка “Toggle Theme” перемикає body:

додає/видаляє клас dark

змінює фон та колір тексту

5. Галерея зображень

Є масив URL-ів картинок.
При кліку по кнопці “Next image” — міняй <img> на наступне фото.
Коли доходиш до кінця — починаєш спочатку.

🔴 HARD (x3)

1. Todo-лист з Done/Delete

Зроби todo-app:

поле input + “Add”

кожна задача:

текст

кнопка Done (додає клас completed)

кнопка Delete (видаляє задачу)

2. Фільтр списку

Є список товарів (5–10 елементів) і input.
Коли вводиш текст — лишаються тільки ті <li>, які містять цей текст (реальне live-фільтрування).

3. Drag & Drop порядок елементів

Зроби список <ul> із 5 пунктів, які можна перетягувати мишкою та міняти порядок (HTML5 drag & drop).
